# Base URL of the simulator backend.
# Dev (separate Vite + Flask): point at the Flask port.
VITE_API_BASE=http://localhost:5000

# Production behind nginx (same origin): leave empty.
# VITE_API_BASE=
